# Second-hand Modebutik Online

## Kort deploy-guide

1. Klona detta repository
2. Installera beroenden med `npm install`
3. Skapa en `.env.local` fil baserad på `.env.example`
4. Kör utvecklingsservern med `npm run dev`
5. Bygg för produktion med `npm run build` och starta servern med `npm start`